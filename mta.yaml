_schema-version: "3.2"
ID: sampleappzmasterdetail
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: sampleappzmasterdetail-destination-content
  type: com.sap.application.content
  requires:
  - name: sampleappzmasterdetail-destination-service
    parameters:
      content-target: true
  - name: sampleappzmasterdetail-repo-host
    parameters:
      service-key:
        name: sampleappzmasterdetail-repo-host-key
  - name: sampleappzmasterdetail-uaa
    parameters:
      service-key:
        name: sampleappzmasterdetail-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: sampleappzmasterdetail_html_repo_host
          ServiceInstanceName: sampleappzmasterdetail-html5-srv
          ServiceKeyName: sampleappzmasterdetail-repo-host-key
          sap.cloud.service: sampleappzmasterdetail
        - Authentication: OAuth2UserTokenExchange
          Name: sampleappzmasterdetail_uaa
          ServiceInstanceName: sampleappzmasterdetail-xsuaa-srv
          ServiceKeyName: sampleappzmasterdetail-uaa-key
          sap.cloud.service: sampleappzmasterdetail
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: sampleappzmasterdetail-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: sampleappzmasterdetail-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - sampleappzmasterdetail.zip
      name: sampleappzmasterdetail
      target-path: resources/
- name: sampleappzmasterdetail
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: sampleappzmasterdetail-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: sampleappzmasterdetail-destination-service
    service-plan: lite
- name: sampleappzmasterdetail-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: sampleappzmasterdetail-xsuaa-srv
    service-plan: application
- name: sampleappzmasterdetail-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: sampleappzmasterdetail-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
